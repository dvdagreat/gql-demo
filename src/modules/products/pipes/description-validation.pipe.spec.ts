import { DescriptionValidationPipe } from './description-validation.pipe';

describe('DescriptionValidationPipe', () => {
  it('should be defined', () => {
    expect(new DescriptionValidationPipe()).toBeDefined();
  });
});
